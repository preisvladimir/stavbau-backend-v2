spring:
  application:
    name: stavbau-backend
  datasource:
    url: jdbc:postgresql://localhost:5432/stavbau
    username: stavbau
    password: stavbau
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
        jdbc:
          lob:
            non_contextual_creation: true
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: false
    baseline-description: "baseline before adopting Flyway"

server:
  port: 8080

app:
  security:
    cookies:
      secure: false
    bcrypt-cost: 12
    jwt:
      issuer: "stavbau"
      accessTokenTtlMinutes: 30
      refreshTokenTtlDays: 14
      secret: "hkCeFiMnK/WnZlQn9JvxguLMB7LEBXxM05E86Gg2Eirg0CWmrKvH1dG0lfcTBwwECiIyw1/Cc4Mgk0qLA6rlpg=="

# <<< PŘESUNUTO DO DEFAULTU
mapycz:
  base-url: "https://api.mapy.com"
  api-key: "ZD4dB8zouM7Zn727KU6u_iTM2pOk31djjAnlqsq3jiQ"  # "${MAPYCZ_API_KEY}"  klíč držíme v ENV / CI secrets
  timeout-ms: 2500

meteostat:
  base-url: "https://meteostat.p.rapidapi.com"
  rapidapi:
    host: "meteostat.p.rapidapi.com"
    key: "440c5b540cmsh1379b04a05dc3eap1fde53jsn01f3e9c63e1e"   # nastav v ENV / docker secrets, NEcommitovat!


---
spring:
  config:
    activate:
      on-profile: dev

logging:
  level:
    org.hibernate.SQL: DEBUG

files:
  storage:
    type: local
    local:
      base-path: ${USER_HOME}/stavbau-data/files
  upload:
    max-size-mb: 25
    allowed-mime:
      - application/pdf
      - image/png
      - image/jpeg
      - text/plain
